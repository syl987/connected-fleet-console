import { MatDialogConfig } from '@angular/material/dialog';

export const dialogOptions: MatDialogConfig = {
  autoFocus: true,
  hasBackdrop: true,
  restoreFocus: true,
  panelClass: ['px-4', 'sm:px-6'],
  width: '100%',
};
